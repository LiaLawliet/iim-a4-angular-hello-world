steps:
  - name: "Init repository"
    uses: actions/checkout@v2
  
  - name: "Setup Node"
    uses: actions/checkout@v2
    with:
      node-version: "14"

  - name: "Install dependencies"
    run: npm install

  - name: "Build app"
    run: npm run build